---
- hosts: cent8
  become: yes
  vars_files:
    - /etc/ansible/vars/user.yml
  tasks:
    - name: Delete user
      user:
        name: "{{ item }}"
        state: absent
        remove: yes
      with_items:
        - "{{ students }}"
        - "{{ faculty }}"
